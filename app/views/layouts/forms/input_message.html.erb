<% message = resource.errors.full_messages_for(field).first %>

<% if (!message.nil?) %>
	<div class="input-message red-text bold small-text-3">
		<i class="material-icons inline red-text">&#xE000;</i>
		<%= message %>
	</div>
<% end %>

